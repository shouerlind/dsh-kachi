# CONTEXT.md — 术语表

ns-notify:dsh 的音效插件,为 dsh 的所有操作提供任天堂 Switch 系统同款音效反馈,「事事有回应」。npm 包名 **dsh-kachi**(dsh- 前缀 + Switch 按键声「カチッ」)。

## 音效与分级

- **音效分级**:每个可发声事件归属三个级别之一。
  - **介入级**:后台标签页也响的音效。保留给需要用户行动或必须知晓的事件。
  - **前台级**:仅页面可见时响的音效。普通操作确认与状态提示。
  - **静默级**:明确不发声的事件。无用户语义,或与其他音效重复。
- **重要通知白名单**:介入级事件的固定清单(权限审批请求、agent 提问、回合错误、任务完成)。后台仅播放白名单内音效;变更白名单需显式决议。
- **Switch 音效槽位**:按语义命名的音效位(开机音、通知重要音、任务完成音、错误音、发送音、菜单移动音、按键音、确认音、取消音、新建音、关闭音、恢复音、警示音)。实际音频为 Switch 系统 rip,仅限个人自用。
- **音效池**:槽位可选音效的来源集合,分**默认池**(Switch 原音)与**备选池**(替代音效包)。_Avoid_: 音效库、资源池。
- **单声道槽位**:同槽位新音立即打断上一响、不叠加的槽位(菜单移动音是唯一一个)。_Avoid_: 独占槽位、solo。
- **事件音量系数**:同一槽位内为个别事件单独设定的相对音量,只在低于槽位主档时生效。_Avoid_: 音量倍率、增益、加成。

## 节流与交互

- **节流**:对同类音效在短时间窗内去重的机制,防止工具调用等高频事件连响。
- **交互音**:由用户对界面的直接操作(悬停、点击、键盘焦点移动、关闭面板)直接触发的音效,恒为前台级;与由 agent/系统事件触发的音效相对。_Avoid_:UI 音效、点击音、音效反馈。全站按钮泛化的事件代码 ID:`ui-click`(点击,按键音槽)、`ui-hover`(悬停/键盘焦点,菜单移动音槽)。
- **二级面板**:菜单触发按钮点开的弹出选项面板;面板内每一行称**选项**。_Avoid_:二级菜单、下拉菜单、弹出菜单。代码标识符(槽位/事件 ID、aria 锚点)可用 menu- 前缀,与正式槽位名「菜单移动音」及 `role="menu"` 对齐;_Avoid_ 仅约束散文与文档表述。
- **未选中关闭**:面板在场时通过再点触发器、点面板外或 Escape 关闭面板的操作;与**选中关闭**(选项点击)相对——前者取消音,后者确认音。_Avoid_: 取消关闭、点外关闭。
- **语义锚点**:以 aria/role 等语义属性识别界面交互目标的做法,不用类名或 DOM 结构。_Avoid_: 选择器、CSS 锚点。
- **座席**:composer 输入区的容器,把输入卡片与权限预设、工作区选择等行圈在一起;dsh 全站唯一已知的无标记触发器群在此。_Avoid_: 输入卡片、composer 卡片。
- **泛化目标**:全站范围内的统一交互目标集(所有按钮与可点击项,含会话列表树条目);与「二级面板」路径并列的第二条发声面,新区域无需逐区适配。_Avoid_: 全局按钮、通用锚。
- **自家行**:插件自己注入的设置行;其点击发声由自家路径负责(own-click 事件),泛化目标集整行跳过以免双响。_Avoid_: 设置行、注入行。
- **差分兜底**:对无 aria 标记的触发器,以点击前后面板有无的差分判定开/关的兜底路径。_Avoid_: 轮询、猜测开关。
