node_modules/
*.tsbuildinfo
src/client/pack-manifest.json
.DS_Store
.dsh-mattskillsdeck-cache/
.workbuddy/
*.tgz
vitest.config.ts.timestamp-*.mjs

# lib/ 的 **JS 产物**入库(2026-09-10 起):插件要能经 `github:` 安装
# (dsh 启动器的插件迁移只认 registry/git 来源,对 link: 本地包不生效),而安装期
# 不保证会跑脚本(prepare 可能被拦,或在 --omit=dev 下失败)。所以产物随源码提交,
# **改了 src/ 必须 `npm run build` 并把 lib/ 一起提交**。
#
# sourcemap **不入库**:它内嵌源码原文,而本机 core.autocrlf=true 会让磁盘上的源码
# 带上 CRLF —— esbuild 读磁盘原文,把 CRLF 一起嵌进 map,于是「已提交的 map」与
# 「任何一次重新构建出的 map」永远不同(且 git status 对源码判定干净,查不出原因)。
# 产物同步闸 `npm run check:dist` 只对账 JS,不把这份环境相关的字节当漂移。
lib/*.map
