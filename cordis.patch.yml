# dsh-kachi bundle patch:one host row over the web profile.
# The row name resolves from the profile's own node_modules where
# `dsh plugin --profile web add .` installed this package.
- insert:
    - id: dsh-kachi
      name: dsh-kachi
